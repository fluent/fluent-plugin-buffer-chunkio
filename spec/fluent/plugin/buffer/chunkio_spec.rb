RSpec.describe Fluent::Plugin::Buffer::Chunkio do
  it "has a version number" do
    expect(Fluent::Plugin::Buffer::Chunkio::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
